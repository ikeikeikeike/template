from .settings import *

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.mysql',
        'NAME': '<%= @app_node[:db_name] %>',
        'USER': '<%= @app_node[:db_user] %>',
        'PASSWORD': '<%= @app_node[:db_password] %>',
        'HOST': '<%= @app_node[:db_host] %>',
        'PORT': '',
    }
}

MYAPP_USERNAME = "<%= @app_node[:my_username] %>"
MYAPP_PASSWORD = "<%= @app_node[:my_password] %>"
